/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["fakestoreapi.com", "www.pinpng.com", "i.imgur.com"],
  },
};

module.exports = nextConfig;
